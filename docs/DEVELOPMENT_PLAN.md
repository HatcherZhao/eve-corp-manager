# 滚动开发计划

> 最后更新：2026-09-12。本文是开发范围与进度的持续记录；每次合并功能、发现阻塞或调整优先级时必须同步更新。

## 维护规则

- `[x]` 已完成并已接入本地运行服务；`[~]` 已实现，仍需真实国服数据或端到端验证；`[ ]` 未开始。
- 每项完成后写明交付内容、验证方式和已知限制；不要把“接口可调用”标记为“业务模块已上线”。
- 新需求先补入对应阶段，再开始实现；本文与 `README.md` 的路线图保持一致，本文记录更细的执行状态。
- 国服已开放的官方写入端点属于建设范围；“当前未实现”不能表述为“系统不做”，只有国服未开放或真实授权验证失败的能力才列为不可用。

## 已完成

- [x] 军团租户、本站账号、CEO / 总监 / 成员派生身份与数据隔离。
- [x] 国服角色授权、角色绑定、权限树、主动刷新、重新授权和本站密码找回。
- [~] 本站登录会话：已定位此前 30 分钟无操作即失效的配置，PC 客户端会话与无操作时长均调整为 6 小时；401 统一引导重新登录，工作台不再将登录过期误报为军团归属问题。本地构建通过，待发布后以线上重新登录和 6 小时闲置场景验收。
- [x] 授权链路本地修复：重新授权先退出网易 EVE 登录态；登录验证码不携带残留本站会话。
- [x] 成员管理：成员名册同步、入团 / 离团状态、角色名称、筛选和详情查看。
- [x] 游戏身份视觉：军团工作台展示国服军团徽标和当前绑定角色肖像；成员列表与右上角当前登录用户优先展示国服角色头像，无有效绑定时回退本站默认头像。图片经服务器持久化文件缓存提供：类型图标缓存 365 天、军团徽标 180 天、角色肖像 90 天；上游临时不可用时继续提供旧缓存。
- [x] 成员追踪：最近登录 / 登出、位置、舰船类型；玩家建筑位置使用结构读取权限解析名称。基地没有可靠名称来源，已从界面移除。
- [x] 成员本站权限管理，以及追踪信息的独立查看权限和访问审计。
- [x] 成员组织能力：成员分组与备注、脱敏组织操作记录、成员同步历史、按筛选导出 CSV，以及成员业务角色调整审计。
- [x] 已登录成员会话权限同步：进入 EVE 成员页会刷新本站角色权限，迁移新增权限无需退出重新登录。
- [x] 军团建筑：独立菜单与快照列表已上线，展示本军团玩家建筑的中文类型和星系、状态、在线服务、燃料到期、状态计时、拆锚时间与快照时间。CEO / 总监可手动同步；成员可查看。真实总监授权已于 2026-09-09 成功同步 10 座建筑和 12 条服务状态。角色快照过期时，同步会先复核国服角色，避免将有效总监误判为无权。
- [~] 月矿情报：国服完整分页快照、精炼厂 / 月球 / 星系时间线、关键词和到达状态筛选已上线。列表与详情新增按浏览器本地时区显示的“可开采日（含星期）”；月矿堡备注已改为以军团、月矿堡唯一保存，后续同步和新的月矿记录均会沿用同一备注。CEO / 总监可维护备注，成员可查看；当前国服月矿端点为只读。本地前后端测试已通过，待发布后以真实国服数据验证长期备注回填与跨记录展示。
- [~] 月矿开采统计：已接入国服观察者和账本完整分页同步，并升级为按时间趋势、开采建筑排行、玩家排行与当前军团总览展示；下方保留矿物与角色的可追溯聚合明细。图表仅使用当前租户绑定军团的真实数据，不伪造跨军团比较；筛选会同步作用于全部统计。2026-09-12 已完成生产发布，公网入口与验证码接口验证通过；图表仍待用真实国服账本完成页面验收。
- [x] 游戏内邮件：已实现授权角色个人邮箱的最近 50 封邮件头同步、游戏原生收件箱/已发送/自定义分类与未读数、按需读取正文、打开即同步已读状态、收件人四种官方类型发信、菜单权限与发送审计。侧边栏已改为“游戏内邮件”菜单组，下设收件箱、已发送和写邮件入口，且使用项目内置 SVG 图标。邮件缓存严格按本站用户和角色绑定隔离；发送前二次确认，国服异常标记为结果待确认且不自动重发。已于 2026-09-11 用真实授权验证 41 封收件箱读取、按需正文读取、未读邮件回写为已读、按游戏角色名反查收件人及真实国服发信回执。
- [x] 游戏通知：已归并到“游戏内通信”菜单组并接入 `esi-characters.read_notifications.v1`，按授权角色缓存和自动同步，提供全部、军团与成员、建筑与资产安全、战争与主权、月矿与工业、其他六个阅读分类；列表显示发送方、中文通知类型、中文摘要、时间及详情，未知国服类型保留原类型并进入“其他”。2026-09-11 以重新授权的真实角色同步 500 条通知，覆盖月矿、建筑安全、军团成员、战争/主权与其他分类；国服结构化原文已从列表移至详情，列表根据真实类型生成中文摘要，详情仍保留解码后的原始内容用于核对。
- [~] 国服新闻活动：已实现网易 EVE 国服官网固定栏目（新闻、活动、维护、更新通知、版本更新）的全局安全快照、10 分钟检查、Redis 互斥、成功/失败状态、新闻/版本更新二级菜单与按需正文阅读。正文严格过滤危险 HTML，仅保留可信 HTTPS 链接和网易图片域名；CEO / 总监可手动检查，成员可阅读。后端解析安全测试、前端 lint/类型检查/生产构建通过；待生产环境完成首次抓取与定时同步验证。

## 进行中与待验证

- [~] 线上图片缓存恢复：已定位线上类型、角色和军团图片均未展示的原因是宿主机挂载目录归属 `root`，而 API 以 `appuser`（uid 10001）运行，导致按需从国服拉取后无法落盘。已增加发布前权限准备脚本；类型图标缺图时会显示本站占位图标。待用户明确要求发布后执行脚本并验证首个请求自动下载、持久化和旧缓存回退。
- [~] 基础信息资料体验：物品资料已补充完整详情抽屉（原生图标、六级分类和完整说明），并改为游戏市场式“左侧分类树 + 右侧物品列表”浏览；父分类会包含下级物品，资料源未分类物品独立展示，分类树在资料导入后自动更新。位置资料同样改为星域 → 星座 → 星系导航树，空间站与公开建筑按所属星系在右侧列表展示，并保留位置图标、完整归属字段和详情抽屉；本地 `eslint`、类型检查、生产构建及两类导航树服务测试通过，待发布后以线上资料页面验收。位置资料没有国服单独图片端点，使用资料类型/位置图标而非伪造的物品图片。
- [~] 国服授权完整闭环：已修复新增业务 Scope（例如游戏内邮件）被误判为“授权失效”并清空刷新令牌的问题；同时将角色、军团 ESI 的 401 / 403、模块 Scope 不足和未知上游错误统一降级为可恢复失败，保留刷新令牌并等待后续自动轮换。后台每分钟扫描有效授权：访问令牌在到期前至少一个扫描周期加提前量进入轮换队列，角色事实仍按一小时缓存周期复核。现有授权缺少新增业务 Scope 时，原授权继续自动续期和复核角色权限，仅受影响模块提示补充授权；仅 OAuth Token 端点明确返回 `invalid_grant`、本地令牌身份校验失败、用户在官网解除授权或角色离开军团时，才降级为重新授权。先前错误逻辑已清空测试账号的令牌，需完成一次重新授权恢复；之后继续验证真实角色的令牌轮转、撤销后的降级和服务重启后的持续同步。
- [~] 成员追踪名称回填：已按 ID 类型实现解析；每次同步须以真实角色权限验证建筑名称可见性与上游缓存时间。

## 后续开发顺序

### 阶段三：资源与市场

- [x] 军团资产：当前完整快照、手动同步、位置 / 类型 / 自定义物品名称解析和层级浏览已上线。资产树按星系 → NPC 空间站 / 玩家建筑 → 仓位 → 实际物品箱（可选）→ 物品组织；舰船按“舰船分区 → 装配槽 / 改装槽 / 无人机舱 / 货仓 → 物品”分层，军团办公室按机库分层，资产安全包裹保留原存放机库，本军团玩家建筑按“建筑分区 → 燃料舱、能量槽、服务槽等具体建筑舱位 → 物品”分层。舰船由静态资料中的市场分类识别，不依赖名称或测试数据；海关办公室、玩家建筑和普通太空资产明确区分；无法解析的位置明确归入未解析分组。资产树与详情、军团建筑、成员当前舰船、物品资料和采矿矿物均显示按国服类型 ID 获取的原生图标。
- [x] EVE 静态资料：`evedata.xlsx` 的类型、市场分类、星域/星座/星系、NPC 空间站和公开建筑已持久化到数据库；启动时在资料库为空时自动导入，名称解析与资产分类优先使用数据库资料。每日 04:15（上海时区）自动从 `https://www.ceve-market.org/dumps/evedata.xlsx` 下载校验；文件哈希未变化不写库，下载、解析或入库失败时保留上一版快照。
- [x] 基础信息菜单：新增“物品资料”和“位置资料”二级菜单；两页均支持关键词检索、筛选、分页与 CSV 导出。CEO / 总监可上传完整 `evedata.xlsx`，服务端完成六张工作表校验后在单一事务中原子替换物品与位置资料；普通成员仅能浏览和导出。已以真实账号验证菜单、列表、导出和完整文件导入，当前资料快照为 28,433 个物品类型与 15,190 个位置资料。
- [x] 国服中文名称基准：已将 `docs/evedata.xlsx` 提取为后端离线名称索引。资产与成员追踪优先使用其中的物品、星系、NPC 空间站和公开玩家建筑中文名称；未收录的新 ID 仍由国服 ESI 补齐。本军团建筑自定义名称与资产层级继续以 ESI 为准。
- [x] 资产 CSV 导出：按当前关键词导出全部有效资产，包含类型、位置、仓位、数量和快照有效期；导出不受资产树折叠状态影响，并沿用资产查看权限与租户隔离。
- [ ] 资产估值：待吉他市场价格基线接入后，为资产快照补充参考单价、合计估值与价格时间。
- [x] 军团建筑：状态、服务、燃料到期和风险显示已上线；独立页面支持关键词、状态筛选、分页和手动完整同步。责任人与燃料风险提醒进入运营协同阶段。
- [~] 月矿情报：提取、到达、自然碎裂时间线与月矿堡长期备注已实现；可开采日同时显示日期与星期。本地测试已通过，待发布后以真实国服数据完成长期备注跨同步保留与展示验收。
- [~] 月矿开采统计：时间、开采建筑、玩家与当前军团总览，以及明细追溯已实现；待用真实国服数据完成图表与筛选联调。
- [ ] 吉他物价与矿物估价：买卖盘、历史价格、资产与月矿参考估值、价格提醒。

### 阶段四：运营协同

- [~] 定时同步、数据新鲜度、上游可用性与授权健康检测：已上线资产、建筑、月矿、月矿开采统计、成员名册与成员追踪的统一数据健康状态，并在各业务页右上角以单行状态展示最近同步时间或失败原因；新增持久化自动同步队列，资产每 6 小时、建筑每 30 分钟、月矿/成员每小时、月矿开采统计每 3 小时、授权角色邮件与游戏通知每 10 分钟。自动任务具备首跑随机抖动、租约互斥、同目标合并、上游 `Expires` 下限、失败指数退避和 401/403 暂停；国服客户端所有请求共享 3 秒最小间隔（约 20 次/分钟），分页也受此约束。用户手动同步在当前请求内立即执行，不经过自动队列的冷却与退避；成功后仅重排后续自动任务。已在本地服务完成建表、任务发现及成员名册首轮真实自动同步验证；其他模块继续按各自到期时间执行。
- [ ] 站内通知、Webhook 与游戏内邮件运营能力：在已上线的个人邮箱收发基础上，补充模板、收件人规则、审批、定时发送、失败处置与通知编排。
- [ ] 官方游戏操作中心：联系人、舰队指挥、装配方案、日历响应和导航航点；统一实现授权角色选择、二次确认、幂等、回执和审计，并逐项通过真实国服授权验证。
- [ ] 月矿提醒与动员：提供倒计时、自愿订阅、站内提醒和游戏内邮件触达；不设置排班、负责人、协同任务或考勤。
- [ ] 军团运营看板：聚合成员、资产、建筑、月矿与采矿指标。

### 阶段五：交付质量

- [ ] 前端工作台视觉与交互优化：空状态、加载反馈、移动端与高频操作。
- [ ] 自动化测试：授权、权限、跨军团隔离、同步、错误恢复与回归测试。
- [ ] 部署运维：生产配置模板、备份恢复、日志监控、版本发布和安全巡检。

## 当前优先事项

1. **吉他物价与矿物估价**：建立市场价格采集和矿物估价基线，为资产、月矿和月矿开采统计提供参考值。
2. **资产估值**：为资产快照补充单价、总值与价格时点，并将价格采集结果纳入统一数据新鲜度提示。
3. **站内通知、Webhook 与邮件运营能力**：在已验收的个人游戏邮件和通知读取基础上，补充模板、收件人规则、审批、定时发送、失败处置与通知编排。
