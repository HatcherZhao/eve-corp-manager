# 项目验证记录

更新日期：2026-09-08。本文仅记录实际完成的本地验证；自动化测试和本地浏览器验收不代表真实国服环境联调完成。

## 自动化验证

| 检查 | 结果 |
|---|---|
| 后端构建与单元测试 | Java 17 下 Maven Reactor 构建成功，当前纳入 Surefire 的测试集全部通过 |
| 数据库集成测试 | 启用 `integration-tests` profile 后，当前纳入 Failsafe 的 MySQL/Redis 集成测试全部通过 |
| 租户隔离 | A/B 军团的 HTTP、MySQL 数据和授权归属隔离通过；伪造租户头返回业务 403；Redis db14 验证通过 |
| Java 格式检查 | Spotless 全模块检查通过 |
| 前端类型检查 | Node.js 22，`pnpm typecheck` 通过 |
| 前端代码检查 | `pnpm lint` 通过；现存非阻断 warning 以命令输出为准 |
| 前端生产构建 | `pnpm build` 成功生成生产产物；现存大 chunk 等非阻断提示以命令输出为准 |
| 提交内容检查 | `git diff --check` 通过；敏感值扫描未发现凭据、令牌、私钥或环境文件泄漏 |

后端全量验证使用 Java 17 完成，包含单元测试、Failsafe 集成测试、Spotless 检查和 Reactor 构建。前端验证统一使用仓库要求的 Node.js 22。测试数量、构建模块数和打包模块数会随代码变化，因此本文不固化这些易失真的计数；应以当次命令的退出码和测试报告为准。

## 本地环境与浏览器验收

- 使用本机 MySQL 新建独立业务库，Liquibase 迁移正常执行。
- Redis 固定使用 db14，集成测试和运行时访问正常。
- 后端在 `127.0.0.1:8000`、前端在 `127.0.0.1:5173` 正常启动并可访问；本次未使用 Docker。
- 用户自有 Chrome 与独立浏览器均已确认验证码正常显示，管理员账号可以登录。
- 「EVE 军团」工作台和权限页可正常打开；权限页展示完整权限树、当前已拥有权限、搜索与筛选能力，并在未绑定 EVE 时显示明确的未就绪状态。
- 个人中心已确认存在“绑定 EVE”“刷新游戏权限”“重新授权”入口；国服 SSO 未配置时会显示明确配置提示。

## 发布前外部门禁

以下条件依赖真实国服资源，当前尚未具备，因此不能宣称真实国服授权或权限同步联调完成：

- 配置项目自有的国服应用凭据及正式回调地址。
- 准备可用于验证的真实 CEO、总监和普通成员账号。
- 使用上述账号完成首次授权、本站登录、主动刷新、重新授权、撤权、离团与自动降权场景。
- 对照国服真实响应核验军团角色、权限树和租户数据边界。

Nginx 配置目前仅作为同源代理示例，尚未部署公网环境。满足上述外部门禁并完成发布环境安全检查后，方可将真实国服联调标记为通过。
